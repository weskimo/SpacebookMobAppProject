import { StyleSheet } from "react-native";

export default StyleSheet.create({

    loginForm: {
        
        alignSelf: 'center',
        alignContent: 'center',
        justifyContent: 'center',
        alignItems: 'center',
        marginVertical: 100
    },

    text: {
        fontSize: 20,
        fontWeight: 'bold',
        marginVertical: 10
    }
    
});